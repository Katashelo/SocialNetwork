let initialState = {
    friends: [
      { id: 1, name: "Arina" },
      { id: 2, name: "Daria" },
      { id: 3, name: "Stas" },
    ],
  };

const sidebarReducer = (state= initialState, action) => {
    
    
    
    
    return state;
}

export default sidebarReducer;